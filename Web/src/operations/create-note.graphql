mutation createNote($input: CreateNoteInput!) {
  createNote(createNoteInput: $input) {
    content
    contentText
    createdAt
    folder
    id
    priority
    tags
    title
    userId
    views
  }
}
